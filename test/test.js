const testdata = require('./test_data').homePage;
const Main = require('../index');

const expect = require('chai').expect;
const chai = require('chai');
const sinon = require('sinon');
const fs = require('fs');

describe('Event fetch lambda', function () {
    it('should extract the titles from the homepage', (done) => {
        done();
    });

    it('should reject if extracting titles fails', (done) => {
        done();
    });

    it('should extract the summaries from the homepage', (done) => {
        done();
    });

    it('should reject if extracting summaries fails', (done) => {
        done();
    });

    it('should merge titles and summaries', (done) => {
        done();
    });

    it('should fetch the page', (done) => {
        done();
    });

    it('should report fetch errors', (done) => {
        done();
    });
});